# ensemble-values

Value model for the Ensemble protocol — 10 types with MessagePack serialization.

Part of the [Ensemble](https://github.com/samgwise/ensemble) project.

## Usage

```rust
use ensemble_values::Value;

let freq = Value::Float(440.0);
```