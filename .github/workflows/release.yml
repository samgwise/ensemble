name: Release

on:
  push:
    tags:
      - '*-v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --workspace -- --test-threads=1

  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Extract crate name from tag
        id: crate
        run: echo "CRATE_NAME=${GITHUB_REF#refs/tags/}" | sed 's/-v.*$//' >> $GITHUB_OUTPUT
      - name: Login to crates.io
        run: cargo login ${{ secrets.CRATES_IO_TOKEN }}
      - name: Publish crate
        run: cargo publish -p ${{ steps.crate.outputs.CRATE_NAME }}

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: publish
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - name: Extract crate name and version from tag
        id: info
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          CRATE_NAME=${TAG%-v*}
          VERSION=${TAG#*-v}
          echo "CRATE_NAME=$CRATE_NAME" >> $GITHUB_OUTPUT
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ steps.info.outputs.CRATE_NAME }} v${{ steps.info.outputs.VERSION }}
          generate_release_notes: true
