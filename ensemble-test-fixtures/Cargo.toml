[package]
name = "ensemble-test-fixtures"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Language-neutral YAML fixtures for Ensemble protocol conformance testing"
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
